package FIRe.Nodes;

import FIRe.ASTVisitor;

public class ValNode extends ExpressionNode{

    @Override
    public void accept(ASTVisitor v, AbstractNode parent) {
        //Denne burde ikke blive benyttet
    }
}
